<html>
    <style>
        table {
          font-family: arial, sans-serif;
          border-collapse: collapse;
          width: 30%;
        }
        
        td, th {
          border: 1px solid #dddddd;
          text-align: left;
          padding: 8px;
        }
        
        tr:nth-child(even) {
          background-color: #dddddd;
        }
        </style>
    <table>
        <tr>
            <th>Available quizes</th>
        </tr>
        <% all.forEach(function(all){ %>
           <tr> <td><a href="/test?name=<%=all%>"><%- all %></a></td>
            <td><a href="/delete?quiz=<%=all%>" onclick="return confirm('Are you sure you want to delete this item?');">
              <img src="/delete.png" width="20" height="20" /></a></td></tr>
          <%});%>
    </table>
    
    <form method="POST">
    <button type="submit" formaction="/action_two">Homepage</button>
    </form>
</html>